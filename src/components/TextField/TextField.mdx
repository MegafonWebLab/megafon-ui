---
name: TextField
menu: Components
---
import { Playground, PropsTable } from 'docz';
import TextField from './TextField';

export const wrapper = (width = 200) => ({ children }) => {
    return (
        <div style={{width: `${width}px`}}>
            {children}
        </div>
    )
}

# TextField

## Properties

<PropsTable of={TextField} />

## Basic usage

<Playground wrapper={wrapper()}>
    <TextField name="name"/>
</Playground>

## Mask usage

<Playground wrapper={wrapper(150)}>
    <TextField mask="(999) 999-99-99" maskChar=" " />
</Playground>

## Error usage

<Playground wrapper={wrapper(150)}>
    <TextField error={true} noticeText="Name is required" />
</Playground>

## Valid usage

<Playground wrapper={wrapper(150)}>
        <TextField valid={true} />
</Playground>

## Comment usage

<Playground wrapper={wrapper(150)}>
    <TextField commentText="Comemnt" />
</Playground>

## Success text usage

<Playground wrapper={wrapper(150)}>
    <TextField successText="Form successfully submitted" />
</Playground>

## Disable usage

<Playground wrapper={wrapper(150)}>
    <TextField disabled={true} />
</Playground>
