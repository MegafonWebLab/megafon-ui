sudo: false
language: node_js

node_js:
  - 10

cache:
  directories:
  - node_modules

before_script:
  - npm install -g codecov
  - npm ci

script:
  - npm run lint
  - npm run coverage
  - npm run docs:build

