---
name: Search
menu: Components
route: components/search
---

import { Playground, Props } from 'docz';
import Search from './Search';

# Search

## Properties

<Props of={Search} />

## Basic usage

<Playground>
    <Search
        placeholder="Type here and press ENTER"
        onSubmit={(searchQuery) => alert('submitting ' + searchQuery)}
    />
</Playground>

## Usage without icon

<Playground>
    <Search hideIcon />
</Playground>

## Usage with initial items

<Playground>
    <Search
        placeholder="Type here for search"
        onSubmit={(searchQuery) => alert('submitting ' + searchQuery)}
        items={[
            {
                title: 'title',
                value: 'value',
            },
            {
                title: 'title2',
                value: 'value2',
            },
        ]}
    />
</Playground>

## Usage with initial value

<Playground>
    <Search
        placeholder="Type here for search"
        value="Previous request"
        onSubmit={(searchQuery) => alert('submitting ' + searchQuery)}
    />
</Playground>

## Usage with onChange callback

<Playground>
    <Search
        placeholder="Type here for search"
        onChange={(searchQuery) => alert('changing ' + searchQuery)}
    />
</Playground>

## Usage with emulating incomingData

<Playground>
      {() => {
        const [items, setItems] = React.useState([]);
        const [chosenValue, setValue] = React.useState('');
        const onChange = React.useCallback((query) => {
                if (!query) {
                    return;
                }
                setItems((oldItems) => [...oldItems, { title: query, value: query }]);
            }, [items, setItems]);
        const onSubmit = React.useCallback((value) => setValue(value), [chosenValue, setValue]);
        return (
            <Search
                value={chosenValue}
                items={items}
                placeholder="Type and wait for results"
                onChange={onChange}
                onSubmit={onSubmit}
            />
        )
      }}
</Playground>

