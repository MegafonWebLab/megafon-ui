import { Playground } from 'docz';
import Button from '../Button';
import Balance from '@megafon/icons/dist/basic-24-balance_24.svg';
import { blockStyle } from './Button.docz';
import { createRef } from "react";
export const ref = createRef();

## Зеленая тема

<Playground style={blockStyle}>
    <Button>active</Button>
    <Button disabled>disabled</Button>
    <Button showLoader>with loader</Button>
    <Button showArrow>with arrow</Button>
    <Button icon={<Balance />}>with icon</Button>
    <div>
        <Button icon={<Balance />} />
    </div>
</Playground>

## Фиолетовая тема

<Playground style={blockStyle}>
    <Button theme="purple">active</Button>
    <Button theme="purple" disabled>disabled</Button>
    <Button theme="purple" showLoader>with loader</Button>
    <Button theme="purple" showArrow>with arrow</Button>
    <Button theme="purple" icon={<Balance />}>with icon</Button>
    <div>
        <Button theme="purple" icon={<Balance />} />
    </div>
</Playground>

## Белая тема

<Playground style={blockStyle}>
    <Button theme="white">active</Button>
    <Button theme="white" disabled>disabled</Button>
    <Button theme="white" showLoader>with loader</Button>
    <Button theme="white" showArrow>with arrow</Button>
    <Button theme="white" icon={<Balance />}>with icon</Button>
    <div>
        <Button theme="white" icon={<Balance />} />
    </div>
</Playground>


## Зеленая тема с outline

<Playground style={blockStyle}>
    <Button type="outline">active</Button>
    <Button type="outline" disabled>disabled</Button>
    <Button type="outline" showLoader>with loader</Button>
    <Button type="outline" showArrow>with arrow</Button>
    <Button type="outline" icon={<Balance />}>with icon</Button>
    <div>
        <Button type="outline" icon={<Balance />} />
    </div>
</Playground>

## Фиолетовая тема с outline

<Playground style={blockStyle}>
    <Button type="outline" theme="purple">active</Button>
    <Button type="outline" theme="purple" disabled>disabled</Button>
    <Button type="outline" theme="purple" showLoader>with loader</Button>
    <Button type="outline" theme="purple" showArrow>with arrow</Button>
    <Button type="outline" theme="purple" icon={<Balance />}>with icon</Button>
    <div>
        <Button type="outline" theme="purple" icon={<Balance />} />
    </div>
</Playground>

## Белая тема с outline

<Playground style={{...blockStyle, backgroundColor: '#333333', color: '#ffffff'}}>
    <Button type="outline" theme="white">active</Button>
    <Button type="outline" theme="white" disabled>disabled</Button>
    <Button type="outline" theme="white" showLoader>with loader</Button>
    <Button type="outline" theme="white" showArrow>with arrow</Button>
    <Button type="outline" theme="white" icon={<Balance />}>with icon</Button>
    <div>
        <Button type="outline" theme="white" icon={<Balance />} />
    </div>
</Playground>

## Черная тема с outline

<Playground style={blockStyle}>
    <Button type="outline" theme="black">active</Button>
    <Button type="outline" theme="black" disabled>disabled</Button>
    <Button type="outline" theme="black" showLoader>with loader</Button>
    <Button type="outline" theme="black" showArrow>with arrow</Button>
    <Button type="outline" theme="black" icon={<Balance />}>with icon</Button>
    <div>
        <Button type="outline" theme="black" icon={<Balance />} />
    </div>
</Playground>

## Размер кнопки

<Playground style={blockStyle}>
    <Button sizeAll="small" showArrow>Small</Button>
    <Button sizeAll="medium" icon={<Balance />}>Medium</Button>
    <Button sizeAll="large">Large</Button>
    <Button sizeDesktop="large" sizeTablet="medium" sizeMobile="small" showArrow>Adaptive</Button>
    <div>
        <Button sizeDesktop="large" sizeTablet="medium" sizeMobile="small" icon={<Balance />} />
    </div>
</Playground>

## Размер лоадера в зависимости от размера кнопки

<Playground style={blockStyle}>
    <Button sizeAll="small" showLoader>Small</Button>
    <Button sizeAll="medium" showLoader>Medium</Button>
    <Button sizeAll="large" showLoader>Large</Button>
    <Button sizeDesktop="large" sizeTablet="medium" sizeMobile="small" showLoader>Adaptive</Button>
</Playground>

## В полную ширину

<Playground style={{...blockStyle, gridTemplateColumns: '1fr'}}>
    <Button fullWidth>Full Width Button</Button>
</Playground>

## Ссылка

<Playground style={blockStyle}>
    <Button href="#">Link</Button>
    <Button href="#" disabled>Disabled Link</Button>
    <Button href="#" target="_blank">Target Link</Button>
    <Button href="#" download>Download Link</Button>
</Playground>

## Ссылка на элемент

<Playground style={blockStyle}>
    <Button buttonRef={ref}>Ref</Button>
</Playground>
