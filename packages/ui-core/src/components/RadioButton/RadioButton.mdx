---
name: RadioButton
menu: Components
---

import { Fragment } from 'react';
import { Playground, PropsTable } from 'docz';
import RadioButton from './RadioButton';

export const flexStyle = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-around',
}

# RadioButton

## Properties

<PropsTable of={RadioButton} />

export class DemoRadioButtonWrapper extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
            selectedOption: '',
        }
    }
    handleChange = value => {
        this.setState({ selectedOption: value });
    }
    render() {
        const { selectedOption } = this.state;
        return (
            <>
                <p align="center"><b>Value:</b> "{selectedOption || undefined}"</p><br />
                {this.props.children({
                    onChange: this.handleChange,
                    selectedOption: this.state.selectedOption,
                })}
            </>
        )
    }
}

``` javascript
export class DemoRadioButtonWrapper extends React.Component {
    constructor(props) {
        super(props);

        this.state = {
            selectedOption: '',
        }
    }

    handleChange = value => {
        this.setState({ selectedOption: value });
    }

    render() {
        const { selectedOption } = this.state;

        return (
            <>
                <p align="center"><b>Value:</b> "{selectedOption || undefined}"</p><br />
                {this.props.children({
                    onChange: this.handleChange,
                    selectedOption: this.state.selectedOption,
                })}
            </>
        )
    }
}
```

## Different states of radio button

<Playground style={flexStyle}>
    <RadioButton value="not-checked-value" isChecked={false}>Not checked</RadioButton>
    <RadioButton value="checked-value" isChecked>Checked</RadioButton>
    <RadioButton value="disabled-value" isDisabled>Disabled</RadioButton>
</Playground>

## Radio buttons with controlled checked state

<Playground>
    <DemoRadioButtonWrapper>
        {({ onChange, selectedOption }) => (
            <div>
                <div style={flexStyle}>
                    <RadioButton value="value-1" isChecked={selectedOption === "value-1"} onChange={onChange}>First radio button</RadioButton>
                    <RadioButton value="value-2" isChecked={selectedOption === "value-2"} onChange={onChange}>Second radio button</RadioButton>
                </div>
            </div>
        )}
    </DemoRadioButtonWrapper>
</Playground>

## Radio buttons with uncontrolled checked state

<Playground>
    <DemoRadioButtonWrapper>
        {({ onChange, selectedOption }) => (
            <div>
                <div style={flexStyle}>
                    <RadioButton value="value-1" name="group-1" onChange={onChange}>First radio button</RadioButton>
                    <RadioButton value="value-2" name="group-1" onChange={onChange}>Second radio button</RadioButton>
                </div>
            </div>
        )}
    </DemoRadioButtonWrapper>
</Playground>

## Label size

<Playground style={flexStyle}>
    <RadioButton name="group-2" value="value-1" textSize="small">First radio button</RadioButton>
    <RadioButton name="group-2" value="value-2" textSize="medium">Second radio button</RadioButton>
</Playground>
