---
name: RadioButton
menu: Components
---

import { Fragment } from 'react';
import { Playground, PropsTable } from 'docz';
import RadioButton from './RadioButton';

export const flexStyle = {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-around',
}

# RadioButton

## Properties

<PropsTable of={RadioButton} />

export class RadioButtonWrapper extends React.Component {
    constructor(props) {
        super(props);
        this.state = {
            selectedOption: '',
        }
    }
    handleChange = value => {
        this.setState({ selectedOption: value });
    }
    render() {
        return this.props.children({
            onChange: this.handleChange,
            selectedOption: this.state.selectedOption,
        })
    }
}

## Different states of radio button

<Playground>
    <RadioButtonWrapper>
        {({ onChange, selectedOption }) => (
            <div style={flexStyle}>
                <RadioButton value="value-1">btn-1</RadioButton>
                <RadioButton value="value-2" isChecked>btn-2</RadioButton>
                <RadioButton value="value-3" isDisabled>btn-3</RadioButton>
            </div>
        )}
    </RadioButtonWrapper>
</Playground>

## Working example of the radio button with prop 'isChecked'

<Playground>
    <RadioButtonWrapper>
        {({ onChange, selectedOption }) => (
            <div style={flexStyle}>
                <RadioButton value="value-1" isChecked={selectedOption === "value-1"} onChange={onChange}>btn-1</RadioButton>
                <RadioButton value="value-2" isChecked={selectedOption === "value-2"} onChange={onChange}>btn-2</RadioButton>
            </div>
        )}
    </RadioButtonWrapper>
</Playground>

## Working example of the radio button without prop 'isChecked'

<Playground>
    <RadioButtonWrapper>
        {({ onChange }) => (
            <div style={flexStyle}>
                <RadioButton value="value-1" name="group-1" onChange={onChange}>btn-1</RadioButton>
                <RadioButton value="value-2" name="group-1" onChange={onChange}>btn-2</RadioButton>
            </div>
        )}
    </RadioButtonWrapper>
</Playground>

## Label size

<Playground style={flexStyle}>
    <RadioButton name="group-2" value="value-1" textSize="small">btn-1</RadioButton>
    <RadioButton name="group-2" value="value-2" textSize="medium">btn-1</RadioButton>
</Playground>
