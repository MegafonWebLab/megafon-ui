import { Props } from 'docz';
import Carousel from '../Carousel';

<Props of={Carousel} />

``` javascript
type SlidesSettingsType = {
    [key: number]: {
        slidesPerView: number | 'auto';
        spaceBetween: number;
    };
};
```
