import { Props } from 'docz';
import Carousel from '../Carousel';

<Props of={Carousel} />

``` javascript
type SlidesSettingsType = {
    [key: number]: {
        slidesPerView: number | 'auto';
        spaceBetween: number;
    };
};
```

За подробной информацией по некоторым свойствам нужно смотреть документацию [swiper](https://swiperjs.com/swiper-api).
