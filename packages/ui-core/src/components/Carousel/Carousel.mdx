---
name: Carousel
menu: Components
---

import { Playground, PropsTable } from 'docz';
import Carousel from './Carousel';
import Logo from 'components/Logo/Logo';
import MegafonTv from 'icons/Service-logos/24/MF-TV.svg';

export const Wrapper = ({children}) => <div>{children}</div>;
//    variableWidth: true,

export const carouselParams = {
      dots: true,
      arrows: true,
      infinite: false,
      speed: 500,
      slidesToShow: 3,
      slidesToScroll: 2,
      responsive: [
          {
              breakpoint: 1023,
              settings: {
                  slidesToShow: 2,
                  slidesToScroll: 1,
                  arrows: true
              }
          },
          {
              breakpoint: 767,
              settings: {
                  slidesToShow: 1.05,
                  arrows: false
              }
          }
      ]
};

# Carousel

## Properties Carousel
<PropsTable of={Carousel} />

## Basic usage

<Playground wrapper={Wrapper}>
    <Carousel options={carouselParams} onClickNext={() => {}}>
        <div style={{ width: 260 }}>
            1<Logo />
        </div>
        <div style={{ width: 260 }}>
            2<Logo />
        </div>
        <div style={{ width: 260 }}>
            3<Logo />
        </div>
        <div style={{ width: 260 }}>
            4<Logo />
        </div>
        <div style={{ width: 260 }}>
            5<Logo />
        </div>
        <div style={{ width: 260 }}>
            6<Logo />
        </div>
        <div style={{ width: 260 }}>
            7<Logo />
        </div>
        <div style={{ width: 260 }}>
            8<Logo />
        </div>
    </Carousel>
</Playground>

## Included props

``` javascript
const carouselParams = {
    dots: false,
    infinite: false,
    speed: 500,
    slidesToShow: 4,
    slidesToScroll: 1,
    responsive: [
        {
            breakpoint: 1024,
            settings: {
                slidesToShow: 3,
            }
        },
        {
            breakpoint: 720,
            settings: {
            slidesToShow: 2,
                dots: true,
            }
        },
        {
            breakpoint: 479,
            settings: {
                slidesToShow: 1,
                dots: true,
            }
        },
    ]
};
```
