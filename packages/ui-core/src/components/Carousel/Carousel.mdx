---
name: Carousel
menu: Components
---

import { Playground, Props } from 'docz';
import Carousel from './Carousel';
import Logo from 'components/Logo/Logo';
import MegafonTv from 'icons/Service-logos/24/MF-TV.svg';
import { carouselParams, CarouselWrapper } from './Carousel.docz';

# Carousel

## Properties Carousel
<Props of={Carousel} />

## Basic usage, default theme

<Playground>
    <CarouselWrapper>
        <Carousel options={carouselParams} onClickNext={() => {}}>
            {new Array(9).fill(undefined).map(() => (
                <div style={{ width: 260 }}>
                    <Logo />
                </div>
            ))}
        </Carousel>
    </CarouselWrapper>
</Playground>

## Basic usage, theme landing, white arrows

<Playground wrapper={Wrapper}>
    <Carousel options={carouselParams} onClickNext={() => {}} theme="landing" arrowColor="white">
        <div style={{ width: 260 }}>
            <Logo />
        </div>
        <div style={{ width: 260 }}>
            <Logo />
        </div>
        <div style={{ width: 260 }}>
            <Logo />
        </div>
        <div style={{ width: 260 }}>
            <Logo />
        </div>
        <div style={{ width: 260 }}>
            <Logo />
        </div>
    </Carousel>
</Playground>

## Basic usage, theme landing, padding beetween slides disabled

<Playground wrapper={Wrapper}>
    <Carousel options={carouselParams} onClickNext={() => {}} theme="landing" arrowColor="white" disablePaddingBetweenSlides>
        <div style={{ width: 260 }}>
            <Logo />
        </div>
        <div style={{ width: 260 }}>
            <Logo />
        </div>
        <div style={{ width: 260 }}>
            <Logo />
        </div>
        <div style={{ width: 260 }}>
            <Logo />
        </div>
        <div style={{ width: 260 }}>
            <Logo />
        </div>
    </Carousel>
</Playground>

## Included props

``` javascript
const carouselParams = {
    dots: false,
    infinite: false,
    speed: 500,
    slidesToShow: 4,
    slidesToScroll: 1,
    responsive: [
        {
            breakpoint: 1024,
            settings: {
                slidesToShow: 3,
            }
        },
        {
            breakpoint: 720,
            settings: {
            slidesToShow: 2,
                dots: true,
            }
        },
        {
            breakpoint: 479,
            settings: {
                slidesToShow: 1,
                dots: true,
            }
        },
    ]
};
```
