import { Playground, Props } from "docz";
import Notification from "../Notification";
import { DemoNotificationWrapper, wrapperStyle } from "./Notification.docz";
import Rating1 from "icons/System/24/Rating_1_24.svg";
import Sended from "icons/System/16/Sended_16.svg";
import Cancel from "icons/System/16/Cancel_16.svg";
import Send from "icons/System/24/Send_24.svg";

```javascript collapse=Код DemoNotificationWrapper
export const DemoNotificationWrapper: React.FC<IDemoNotificationWrapperProps> = ( {
    initialClickAmount = 0,
    children,
} ) => {
    const [cliickAmount, setClickAmount] = useState(initialClickAmount);

    return (
        <div>
            {children({
                onLinkClick: () => setClickAmount(cliickAmount + 1),
                onClose: () => setClickAmount(cliickAmount + 1),
                initialClickAmount: cliickAmount,
            })}
        </div>
    );
};
```

## Типы уведомлений

<Playground style={wrapperStyle}>
   <Notification
      type="info"
      title="Info"
      shadowLevel="zero"
   >
      Уведомление типа info
   </Notification>

   <Notification 
      type="success"
      title="Success"
      shadowLevel="zero"
   >
      Уведомление типа success
   </Notification>

   <Notification 
      type="warning"
      title="Warning"
      shadowLevel="zero"
   >
      Уведомление типа warning
   </Notification>

   <Notification 
      type="error" 
      title="Error"
      shadowLevel="zero"
   >
      Уведомление типа error
   </Notification>
</Playground>

## На белом фоне

<Playground style={wrapperStyle}>
   <Notification 
      type="info"
      title="Info"
      shadowLevel="high"
      isColored={false}
   >
      Уведомление типа info с белым фоном
   </Notification>

   <Notification 
      type="success" 
      title="Success"
      shadowLevel="high"
      isColored={false}
   >
      Уведомление типа success с цветным фоном и с типом тени Hover
   </Notification>

   <Notification 
      type="warning"
      title="Warning"
      shadowLevel="high"
      isColored={false}
   >
      Уведомление типа warning с белым фоном
   </Notification>

   <Notification 
      type="error"
      title="Error"
      shadowLevel="high"
      isColored={false}
   >
      Уведомление типа error с белым фоном
   </Notification>
</Playground>

## Тени

<Playground style={wrapperStyle}>
   <Notification 
      type="info"
      title="shadowLevel=zero"
      shadowLevel="zero"
   >
      Уровень тени zero
   </Notification>

   <Notification 
      type="info"
      title="shadowLevel=low"
      shadowLevel="low"
      isColored={false}
   >
      Уровень тени low
   </Notification>

   <Notification 
      type="info"
      title="shadowLevel=high"
      shadowLevel="high"
      isColored={false}
   >
      Уровень тени high
   </Notification>

   <Notification 
      type="info"
      title="shadowLevel=hover"
      shadowLevel="hover"
   >
      Уровень тени hover
   </Notification>
</Playground>

## Кастомные иконки

<Playground style={wrapperStyle}>
   <Notification 
      type="info"
      title="Info"
      shadowLevel="zero"
      icon={<Send />} 
   >
      Фоновый цвет и заливка иконок меняется в зависимости от типа и фонового цвета уведомления
   </Notification>

   <Notification 
      type="info"
      title="Info"
      shadowLevel="high"
      isColored={false}
      icon={<Send />} 
   >
      Фоновый цвет и заливка иконок меняется в зависимости от типа и фонового цвета уведомления
   </Notification>

   <Notification 
      type="success"
      title="Success"
      shadowLevel="zero"
      icon={<Sended />}
   >
      Фоновый цвет и заливка иконок меняется в зависимости от типа и фонового цвета уведомления
   </Notification>

   <Notification 
      type="success"
      title="Success"
      shadowLevel="high"
      isColored={false}
      icon={<Sended />}
   >
      Фоновый цвет и заливка иконок меняется в зависимости от типа и фонового цвета уведомления
   </Notification>

   <Notification 
      type="warning"
      title="Warning"
      shadowLevel="zero"
      icon={<Rating1 />} 
   >
      Фоновый цвет и заливка иконок меняется в зависимости от типа и фонового цвета уведомления
   </Notification>

   <Notification 
      type="warning"
      title="Warning"
      shadowLevel="high"
      isColored={false}
      icon={<Rating1 />}
   >
      Фоновый цвет и заливка иконок меняется в зависимости от типа и фонового цвета уведомления
   </Notification>

   <Notification 
      type="error"
      title="Error"
      shadowLevel="zero"
      icon={<Cancel />}
   >
      Фоновый цвет и заливка иконок меняется в зависимости от типа и фонового цвета уведомления
   </Notification>

   <Notification 
      type="error" 
      title="Error"
      shadowLevel="high"
      isColored={false}
      icon={<Cancel />}
   >
      Фоновый цвет и заливка иконок меняется в зависимости от типа и фонового цвета уведомления
   </Notification>
</Playground>

## Уведомления со ссылками

<Playground style={wrapperStyle}>
   <Notification 
      type="info"
      title="Cсылка ведет на новую страницу"
      shadowLevel="high"
      link="Cсылка на новую страницу"
      target="_blank"
      href="/"
      target="_blank"
      isColored={false}
   >
      На персональном счёте недостаточно средств.
   </Notification>

   <DemoNotificationWrapper>
      {({ initialClickAmount, onLinkClick }) =>
         <Notification 
            type="warning"
            title="Cсылка с кастомным обработчиком"
            shadowLevel="zero"
            link="Cсылка с обработчиком"
            onLinkClick={onLinkClick}
         >
            {`Количество кликов: ${initialClickAmount}`}
         </Notification>
      }
   </DemoNotificationWrapper>

   <DemoNotificationWrapper>
      {({ initialClickAmount, onLinkClick }) =>
         <Notification 
            type="error"
            title="Cсылка с кастомным обработчиком"
            shadowLevel="zero"
            link="Cсылка с обработчиком"
            onLinkClick={onLinkClick}
         >
            {`Количество кликов: ${initialClickAmount}`}
         </Notification>
      }
   </DemoNotificationWrapper>
</Playground>

## Наличие кнопки закрытия

<Playground style={wrapperStyle}>
   <DemoNotificationWrapper>
      {({ initialClickAmount, onClose }) =>
         <Notification 
            type="error"
            title="Кнопка закрытия с обработчиком"
            shadowLevel="high"
            hasCloseButton
            isColored={false}
            onClose={onClose}
         >
            {`Количество кликов: ${initialClickAmount}`}
         </Notification>
      }
   </DemoNotificationWrapper>
</Playground>


## Заполнение контентом частично

<Playground style={wrapperStyle}>
   <Notification 
      type="info"
      shadowLevel="high"
      isColored={false}
      link="Cсылка"
      href="/"
      target="_blank"
   >
      Ссылка и отсутствие заголовка.
   </Notification>

   <Notification 
      type="info"
      title="Кнопка закрытия с обработчиком и ссылка на новую страницу"
      shadowLevel="high"
      hasCloseButton
      isColored={false}
      link="Cсылка на новую страницу"
      href="/"
      target="_blank"
   >
      Использованы все опциональные элементы
   </Notification>

   <Notification 
      type="info"
      shadowLevel="zero"
      hasCloseButton
   >
      Отсутствие заголовка и ссылки.<br />
      Если высота текста больше высоты иконки, то они выравниваются по верху контентной области.
   </Notification>

   <Notification 
      type="info"
      shadowLevel="zero"
      hasCloseButton
   >
     Центрирование по вертикали, при небольшой высоте текста. 
   </Notification>
</Playground>