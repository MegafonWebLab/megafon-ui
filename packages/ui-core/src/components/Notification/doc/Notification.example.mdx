import { Playground, Props } from "docz";
import Notification from "../Notification";
import { DemoNotificationWrapper, wrapperStyle } from "./Notification.docz";
import Rating1 from "icons/System/24/Rating_1_24.svg";
import Checked from "icons/System/16/Checked_16.svg";

```javascript collapse=Код DemoNotificationWrapper
export const DemoNotificationWrapper: React.FC<IDemoNotificationWrapperProps> = ( {
    initialClickAmount = 0,
    children,
} ) => {
    const [cliickAmount, setClickAmount] = useState(initialClickAmount);

    return (
        <div>
            {children({
                onLinkClick: () => setClickAmount(cliickAmount + 1),
                onClose: () => setClickAmount(cliickAmount + 1),
                initialClickAmount: cliickAmount,
            })}
        </div>
    );
};
```

## Типы уведомлений

<Playground style={wrapperStyle}>
   <Notification 
      type="error"
      title="Error"
      content="Уведомление типа error"
      shadowLevel="zero"
   />

   <Notification 
      type="warning"
      title="Warning"
      content="Уведомление типа warning"
      shadowLevel="low"
   />

   <Notification 
      type="info"
      title="Info"
      content="Уведомление типа info"
      shadowLevel="high"
   />

   <Notification 
      type="success" 
      title="Success"
      content="Уведомление типа success"
      shadowLevel="hover"
   />
</Playground>

## Уведомления на белом фоне

<Playground style={wrapperStyle}>
   <Notification 
      type="error"
      title="Error"
      content="Уведомление типа error с белым фоном"
      shadowLevel="zero"
      isColored={false}
   />

   <Notification 
      type="warning"
      title="Warning"
      content="Уведомление типа warning с белым фоном"
      shadowLevel="low"
      isColored={false}
   />

   <Notification 
      type="info"
      title="Info"
      content="Уведомление типа info с белым фоном "
      shadowLevel="high"
      isColored={false}
   />

   <Notification 
      type="success" 
      title="Success"
      content="Уведомление типа success с цветным фоном и с типом тени Hover"
      shadowLevel="hover"
      isColored={false}
   />
</Playground>

## Варианты используемых теней

<Playground style={wrapperStyle}>
   <Notification 
      type="info"
      title="shadowLevel=zero"
      content="Уровень тени zero"
      shadowLevel="zero"
   />

   <Notification 
      type="info"
      title="shadowLevel=low"
      content="Уровень тени low"
      shadowLevel="low"
      isColored={false}
   />

   <Notification 
      type="info"
      title="shadowLevel=high"
      content="Уровень тени high"
      shadowLevel="high"
      isColored={false}
   />

   <Notification 
      type="info"
      title="shadowLevel=hover"
      content="Уровень тени hover"
      shadowLevel="hover"
   />
</Playground>

## Уведомления с использованием настраиваемых иконок

<Playground style={wrapperStyle}>
   <Notification 
      type="warning"
      title="Warning"
      content="Фоновый цвет и заливка иконок меняется в зависимости от типа и фонового цвета уведомления"
      shadowLevel="zero"
      icon={<Rating1 />} 
   />

   <Notification 
      type="warning"
      title="Warning"
      content="Фоновый цвет и заливка иконок меняется в зависимости от типа и фонового цвета уведомления"
      shadowLevel="low"
      isColored={false}
      icon={<Rating1 />}
   />

   <Notification 
      type="success"
      title="Success"
      content="Фоновый цвет и заливка иконок меняется в зависимости от типа и фонового цвета уведомления"
      shadowLevel="high"
      isColored={false}
      icon={<Checked />}
   />

   <Notification 
      type="success" 
      title="Success"
      content="Фоновый цвет и заливка иконок меняется в зависимости от типа и фонового цвета уведомления"
      shadowLevel="hover"
      icon={<Checked />}
   />
</Playground>

## Уведомления со ссылками

<Playground style={wrapperStyle}>
   <Notification 
      type="info"
      title="Cсылка ведет на новую страницу"
      content="На персональном счёте недостаточно средств."
      shadowLevel="hover"
      link="Cсылка на новую страницу"
      target="_blank"
      href="/"
      target="_blank"
      isColored={false}
   />

   <DemoNotificationWrapper>
      {({ initialClickAmount, onLinkClick }) =>
         <Notification 
            type="warning"
            title="Cсылка с кастомным обработчиком"
            content={`Количество кликов: ${initialClickAmount}`}
            shadowLevel="low"
            link="Cсылка с обработчиком"
            onLinkClick={onLinkClick}
         />
      }
   </DemoNotificationWrapper>
</Playground>

## Уведомления с кнопкой закрытия

<Playground style={wrapperStyle}>
   <DemoNotificationWrapper>
      {({ initialClickAmount, onClose }) =>
         <Notification 
            type="error"
            title="Кнопка закрытия с обработчиком"
            content={`Количество кликов: ${initialClickAmount}`}
            shadowLevel="low"
            hasCloseButton
            isColored={false}
            onClose={onClose}
         />
      }
   </DemoNotificationWrapper>

   <Notification 
      type="error"
      title="Уведомление без кнопки закрытия"
      content="На персональном счёте недостаточно средств."
      shadowLevel="hover"
      isColored={false}
   />
</Playground>


## Различные варианты отображения компонента

<Playground style={wrapperStyle}>
   <DemoNotificationWrapper>
      {({ initialClickAmount, onLinkClick }) =>
         <Notification 
            type="info"
            content={`Ссылка с обработчиком и отсутствие заголовка. Количество кликов: ${initialClickAmount}`}
            shadowLevel="low"
            isColored={false}
            link="Cсылка с обработчиком"
            onLinkClick={onLinkClick}
         />
      }
   </DemoNotificationWrapper>

   <DemoNotificationWrapper>
      {({ initialClickAmount, onClose }) =>
         <Notification 
            type="info"
            content={`Отсутствие заголовка и ссылки. Количество кликов закрытия: ${initialClickAmount}`}
            shadowLevel="low"
            hasCloseButton
            onClose={onClose}
         />
      }
   </DemoNotificationWrapper>

   <DemoNotificationWrapper>
      {({ initialClickAmount, onClose }) =>
         <Notification 
            type="info"
            title="Кнопка закрытия с обработчиком и ссылка на новую страницу"
            content={`Количество кликов: ${initialClickAmount}`}
            shadowLevel="low"
            hasCloseButton
            isColored={false}
            link="Cсылка на новую страницу"
            href="/"
            target="_blank"
            onClose={onClose}
         />
      }
   </DemoNotificationWrapper>
</Playground>