import { Playground, Props } from 'docz';
import Checkbox from '../Checkbox';
import Info from 'icons/System/16/Info_16.svg';
import { CheckboxWrapper } from './Checkbox.docz';

```javascript collapse=Код CheckboxWrapper
export const CheckboxWrapper: React.FC<ICheckboxWrapperProps> = (props) => {
    const [checked, setChecked] = React.useState<boolean>(false);

    const handleChange = () => {
        setChecked(!checked);
    };

    return (
        <div style={{ padding: '20px 10px 8px 10px' }}>
            {props.children({
                checked,
                onChange: handleChange,
            })}
        </div>
    );
};
```

## Маленький размер текста

<Playground>
    <CheckboxWrapper>
        {({ checked, onChange }) =>
            <Checkbox
                checked={checked}
                onChange={onChange}
                fontSize="small"
            >
                I agree
            </Checkbox>
        }
    </CheckboxWrapper>
</Playground>

## С ошибкой

<Playground>
    <CheckboxWrapper>
        {({ checked, onChange }) =>
            <Checkbox checked={checked} onChange={onChange} error>
                I agree
            </Checkbox>
        }
    </CheckboxWrapper>
</Playground>

## Светлая тема на темном фоне

<Playground style={{ backgroundColor: '#00b956' }}>
        <CheckboxWrapper>
            {({ checked, onChange }) =>
                <Checkbox
                    checked={checked}
                    onChange={onChange}
                    color="light"
                >
                    I agree
                </Checkbox>
            }
        </CheckboxWrapper>
</Playground>

## С дополнительным контентом справа

<Playground>
    <CheckboxWrapper>
        {({ checked, onChange }) =>
            <Checkbox
                checked={checked}
                onChange={onChange}
                extraContent={
                    <div style={{ backgroundColor: '#00B956', color: '#FFF', padding: '5px'}}>
                        Some Extra Content
                    </div>
                }
            >
                I agree
            </Checkbox>
        }
    </CheckboxWrapper>
</Playground>

## Недоступное взаимодействие с компонентом

<Playground>
    <CheckboxWrapper>
        {() => <Checkbox disabled>I agree</Checkbox>}
    </CheckboxWrapper>
</Playground>

## Недоступное взаимодействие с компонентом + состояние checked

<Playground>
    <CheckboxWrapper>
        {() => <Checkbox checked disabled>I agree</Checkbox>}
    </CheckboxWrapper>
</Playground>
