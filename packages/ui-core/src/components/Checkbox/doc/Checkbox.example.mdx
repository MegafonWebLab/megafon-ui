import { Playground, Props } from 'docz';
import Checkbox from '../Checkbox';
import Info from 'icons/System/16/Info_16.svg';
import { CheckboxWrapper, LightColorWrapper } from './Checkbox.docz';

<Playground>
    <CheckboxWrapper>
        {({ checked, onChange }) =>
            <Checkbox checked={checked} onChange={onChange}>
                I agree
            </Checkbox>
        }
    </CheckboxWrapper>
</Playground>

## Маленький размер текста

<Playground>
    <CheckboxWrapper>
        {({ checked, onChange }) =>
            <Checkbox
                checked={checked}
                onChange={onChange}
                fontSize="small"
            >
                I agree
            </Checkbox>
        }
    </CheckboxWrapper>
</Playground>

## С ошибкой

<Playground>
    <CheckboxWrapper>
        {({ checked, onChange }) =>
            <Checkbox checked={checked} onChange={onChange} error>
                I agree
            </Checkbox>
        }
    </CheckboxWrapper>
</Playground>

## Светлый фон

<Playground>
    <LightColorWrapper>
        <CheckboxWrapper>
            {({ checked, onChange }) =>
                <Checkbox
                    checked={checked}
                    onChange={onChange}
                    color="light"
                >
                    I agree
                </Checkbox>
            }
        </CheckboxWrapper>
    </LightColorWrapper>
</Playground>

## С дополнительным контентов справа

<Playground>
    <CheckboxWrapper>
        {({ checked, onChange }) =>
            <Checkbox
                checked={checked}
                onChange={onChange}
                extraContent={
                    <div style={{ backgroundColor: '#00B956', color: '#FFF', padding: '5px'}}>
                        Some Extra Content
                    </div>
                }
            >
                I agree
            </Checkbox>
        }
    </CheckboxWrapper>
</Playground>

## Отключить взаимодействие с компонентом

<Playground>
    <CheckboxWrapper>
        {() => <Checkbox disabled>I agree</Checkbox>}
    </CheckboxWrapper>
</Playground>

## Отключить взаимодействие с компонентом + состояние checked

<Playground>
    <CheckboxWrapper>
        {() => <Checkbox checked disabled>I agree</Checkbox>}
    </CheckboxWrapper>
</Playground>
