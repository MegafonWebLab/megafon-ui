---
name: Checkbox
menu: Компоненты
route: components/checkbox
---

import { Playground, Props } from 'docz';
import Checkbox from './Checkbox';
import Info from 'icons/System/16/Info_16.svg';
import { CheckboxWrapper, LightColorWrapper } from './Checkbox.docz';

# Checkbox

## Properties

<Props of={Checkbox} />

## Basic usage

<Playground>
    <CheckboxWrapper>
        {({ checked, onChange }) =>
            <Checkbox checked={checked} onChange={onChange}>
                I agree
            </Checkbox>
        }
    </CheckboxWrapper>
</Playground>

## Checkbox with small label size

<Playground>
    <CheckboxWrapper>
        {({ checked, onChange }) =>
            <Checkbox
                checked={checked}
                onChange={onChange}
                fontSize="small"
            >
                I agree
            </Checkbox>
        }
    </CheckboxWrapper>
</Playground>

## Checkbox usage with error

<Playground>
    <CheckboxWrapper>
        {({ checked, onChange }) =>
            <Checkbox checked={checked} onChange={onChange} error>
                I agree
            </Checkbox>
        }
    </CheckboxWrapper>
</Playground>

## Checkbox usage for light color scheme

<Playground>
    <LightColorWrapper>
        <CheckboxWrapper>
            {({ checked, onChange }) =>
                <Checkbox
                    checked={checked}
                    onChange={onChange}
                    color="light"
                >
                    I agree
                </Checkbox>
            }
        </CheckboxWrapper>
    </LightColorWrapper>
</Playground>

## Checkbox with extraContent

<Playground>
    <CheckboxWrapper>
        {({ checked, onChange }) =>
            <Checkbox
                checked={checked}
                onChange={onChange}
                extraContent={
                    <div style={{ backgroundColor: '#00B956', color: '#FFF', padding: '5px'}}>
                        Some Extra Content
                    </div>
                }
            >
                I agree
            </Checkbox>
        }
    </CheckboxWrapper>
</Playground>

## Disabled unchecked checkbox

<Playground>
    <CheckboxWrapper>
        {() => <Checkbox disabled>I agree</Checkbox>}
    </CheckboxWrapper>
</Playground>

## Disabled checked checkbox

<Playground>
    <CheckboxWrapper>
        {() => <Checkbox checked disabled>I agree</Checkbox>}
    </CheckboxWrapper>
</Playground>
