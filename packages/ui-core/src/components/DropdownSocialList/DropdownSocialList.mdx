---
name: DropdownSocialList
menu: Components
---

import { Playground, Props } from "docz";
import DropdownSocialList from "./DropdownSocialList.tsx";
import Checked from "icons/System/24/Checked_24.svg";
import Cancel from "icons/System/24/Cancel_24.svg";

# DropdownSocialList

export const DropdownSocialListWrapper = ({ children }) => {
    return (
        <div
            style={{
                height: "200px",
                display: "flex",
                justifyContent: "center",
                alignItems: "center"
            }}
        >
            {children}
        </div>
    );
};

## Properties

<Props of={DropdownSocialList} />

## Basic usage

<Playground wrapper={DropdownSocialListWrapper}>
    <DropdownSocialList
        icons={[
            {
                svgIcon: <Checked />,
                title: "ok"
            },
            {
                svgIcon: <Cancel />,
                title: "error"
            },
            {
                svgIcon: <Checked />,
                title: "error"
            }
        ]}
        maxIconNumber={2}
    />
</Playground>
