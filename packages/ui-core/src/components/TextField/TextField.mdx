---
name: TextField
menu: Components
---
import { Playground, PropsTable } from 'docz';
import TextField from './TextField';

export const wrapper = (width = 250) => ({ children }) => {
    return (
        <div style={{width: `${width}px`}}>
            {children}
        </div>
    )
}

# TextField

## Properties

<PropsTable of={TextField} />

### Classes property keys
field        | description
-------------|----------
*input*       |  Class that is passed to the input

## API

Method  | Description
------- | ---
blur    | Trigger blur event on input field.
focus   | Trigger focus event on input field.

**Use TextField's 'ref' prop for API methods call:**

```
class SearchForm extends Component {
    this.textField = undefined;

    handleSubmit() {
        textField.blur();
    }

    render() {
        return (
            <Form onSubmit={this.handleSubmit}>
                <TextField ref={(instance) => { this.textField = instance; }} />
            </Form>
        );
    }
}
```

## Basic usage

<Playground wrapper={wrapper()}>
    <TextField name="name"/>
</Playground>

## Mask usage

<Playground wrapper={wrapper()}>
    <TextField
        placeholder="+7 (999) 999-99-99"
        mask="+7 (999) 999-99-99"
        maskChar="_"
        label="Phone number"
        id="2"
        classes={{input: 'parent__inputClass'}}
    />
</Playground>

## Error usage

<Playground wrapper={wrapper()}>
    <TextField error={true} noticeText="Name is required" />
</Playground>

## Valid usage

<Playground wrapper={wrapper()}>
        <TextField valid={true} />
</Playground>

## Comment usage

<Playground wrapper={wrapper()}>
    <TextField commentText="Comment" />
</Playground>

## Success text usage

<Playground wrapper={wrapper()}>
    <TextField successText="Field is correct" />
</Playground>

## Max length text usage

<Playground wrapper={wrapper()}>
    <TextField maxLength={10} />
</Playground>

## Disable usage

<Playground wrapper={wrapper()}>
    <TextField disabled={true} />
</Playground>

## Password type usage

<Playground wrapper={wrapper()}>
    <TextField name="name" placeholder="password" type="password"/>
</Playground>

## Increased size of space between words usage
<Playground wrapper={wrapper()}>
    <TextField bigSpace={true} />
</Playground>
