---
name: TextField
menu: Компоненты
route: components/textfield
---

import { Playground, Props } from "docz";
import TextField from "./TextField";
import { TextFieldWrapper } from "./TextField.docz";

# TextField

## Properties

<Props of={TextField} />

## API

| Method | Description                         |
| ------ | ----------------------------------- |
| blur   | Trigger blur event on input field.  |
| focus  | Trigger focus event on input field. |

**Use TextField's 'ref' prop for API methods call:**

```
class SearchForm extends Component {
    this.textField = undefined;

    handleSubmit() {
        textField.blur();
    }

    render() {
        return (
            <Form onSubmit={this.handleSubmit}>
                <TextField ref={(instance) => { this.textField = instance; }} />
            </Form>
        );
    }
}
```

## Basic usage

<Playground>
    <TextFieldWrapper>
        <TextField name="name" />
    </TextFieldWrapper>
</Playground>

## Mask usage

<Playground>
    <TextFieldWrapper>
        <TextField
            placeholder="+7 (999) 999-99-99"
            mask="+7 (999) 999-99-99"
            maskChar="_"
            label="Phone number"
            id="2"
        />
    </TextFieldWrapper>
</Playground>

## Error usage

<Playground>
    <TextFieldWrapper>
        <TextField error={true} noticeText="Name is required" />
    </TextFieldWrapper>
</Playground>

## Valid usage

<Playground>
    <TextFieldWrapper>
        <TextField valid={true} />
    </TextFieldWrapper>
</Playground>

## Comment usage

<Playground>
    <TextFieldWrapper>
        <TextField commentText="Comment" />
    </TextFieldWrapper>
</Playground>

## Success text usage

<Playground>
    <TextFieldWrapper>
        <TextField successText="Field is correct" />
    </TextFieldWrapper>
</Playground>

## Max length text usage

<Playground>
    <TextFieldWrapper>
        <TextField maxLength={10} />
    </TextFieldWrapper>
</Playground>

## Disable usage

<Playground>
    <TextFieldWrapper>
        <TextField disabled={true} />
    </TextFieldWrapper>
</Playground>

## Password type usage

<Playground>
    <TextFieldWrapper>
        <TextField name="name" placeholder="password" type="password" />
    </TextFieldWrapper>
</Playground>

## Increased size of space between words usage

<Playground>
    <TextFieldWrapper>
        <TextField bigSpace={true} />
    </TextFieldWrapper>
</Playground>
