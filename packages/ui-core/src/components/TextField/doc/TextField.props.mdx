import TextField from '../TextField';
import { Props } from 'docz';

<Props of={TextField} />

```javascript
interface IMaskSelection {
    // Начальная позиция выбора строки внутри инпута
    start: number;
    // Конечная позиция выбора строки внутри инпута
    end: number;
}

export interface IMaskState {
    // Значение маски в маскированном формате, либо текущее, либо предыдущее, в зависимости от свойства
    value: string;
    // Обозначение какая часть значения в инпуты была выбрана, если не выбрано ничего, возвращает
    // два одинаковых значения, обозначающих положение курсора в инпуте
    selection: IMaskSelection;
}
```
