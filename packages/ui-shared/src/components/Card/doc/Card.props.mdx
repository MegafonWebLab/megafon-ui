import { Props } from 'docz';
import Card from '../Card';

<Props of={Card} />

```javascript

enum TargetType = {
    SELF: '_self',
    BLANK: '_blank',
};

interface IButton {
     /** Текст кнопки */
    title: string;
    /** Ссылка в кнопке */
    href: string;
    /** Target - атрибут тега <a> */
    target: TargetType;
    /** Rel - атрибут тега <a> */
    rel?: string;
    /** Обработчик клика по кнопке */
    onClick: () => void;
}

interface ILink {
    /** Текст ссылки */
    title: string;
    /** Адрес ссылки */
    href?: string;
    /** Target - атрибут тега <a> */
    target: TargetType;
    /** Rel - атрибут тега <a> */
    rel?: string;
}

```
