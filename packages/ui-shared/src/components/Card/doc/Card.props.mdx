import { Props } from 'docz';
import Card from '../Card';

<Props of={Card} />

```javascript

interface IButton {
     /** Текст кнопки */
    title: string;
    /** Ссылка в кнопке */
    href: string;
}

interface ILink {
    /** Текст ссылки */
    title: string;
    /** Адрес ссылки */
    href?: string;
}

```
