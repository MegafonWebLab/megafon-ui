import { Props } from 'docz';
import Instructions from '../Instructions';

<Props of={Instructions} />

``` javascript

type InstructionItemType = {
    title: string | React.ReactNode | React.ReactNode[];;
    mediaUrl: string;
    isVideo: boolean;
};

```
