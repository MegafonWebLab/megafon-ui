import { Playground } from 'docz';
import StoreBanner from '../StoreBanner';
import { title, text, htmlTitle, htmlText, image, qrCode, newIphoneImage, link, onClick } from './StoreBanner.docz.tsx';
import { ContentArea } from '@megafon/ui-core';

```javascript collapse=Демо данные

const title = 'Скачивайте мобильное приложение МегаФон';
const text = 'Скачай и получи максимум';
const link = '#';
const onClick = () => {};

```

## Базовое использование

<Playground>
    <ContentArea>
        <StoreBanner
            title={title}
            text={text}
            linkApple={link}
            onClickApple={onClick}
            linkGoogle={link}
            onClickGoogle={onClick}
            imageSrc={image}
            deviceMask="android"
        />
    </ContentArea>
</Playground>

## Шаблоны девайсов

### Новый iphone

<Playground>
    <ContentArea>
        <StoreBanner
            title={title}
            text={text}
            linkApple={link}
            onClickApple={onClick}
            linkGoogle={link}
            onClickGoogle={onClick}
            imageSrc={newIphoneImage}
            deviceMask="new-iphone"
        />
    </ContentArea>
</Playground>

### Белый iphone

<Playground>
    <ContentArea>
        <StoreBanner
            title={title}
            text={text}
            linkApple={link}
            onClickApple={onClick}
            linkGoogle={link}
            onClickGoogle={onClick}
            imageSrc={image}
            deviceMask="white-iphone"
        />
    </ContentArea>
</Playground>

### Черный iphone

<Playground>
    <ContentArea>
        <StoreBanner
            title={title}
            text={text}
            linkApple={link}
            onClickApple={onClick}
            linkGoogle={link}
            onClickGoogle={onClick}
            imageSrc={image}
            deviceMask="black-iphone"
        />
    </ContentArea>
</Playground>

### Android

<Playground>
    <ContentArea>
        <StoreBanner
            title={title}
            text={text}
            linkApple={link}
            onClickApple={onClick}
            linkGoogle={link}
            onClickGoogle={onClick}
            imageSrc={image}
            deviceMask="android"
        />
    </ContentArea>
</Playground>

## Цветовые темы

### Зеленая

<Playground>
    <ContentArea>
        <StoreBanner
            title={title}
            text={text}
            linkApple={link}
            onClickApple={onClick}
            linkGoogle={link}
            onClickGoogle={onClick}
            imageSrc={image}
            theme="green"
            deviceMask="white-iphone"
        />
    </ContentArea>
</Playground>

### Серая

<Playground>
    <ContentArea>
        <StoreBanner
            title={title}
            text={text}
            linkApple={link}
            onClickApple={onClick}
            linkGoogle={link}
            onClickGoogle={onClick}
            imageSrc={image}
            theme="spbSky1"
            deviceMask="android"
        />
    </ContentArea>
</Playground>

## QR-код

### 1 стор

<Playground>
    <ContentArea>
        <StoreBanner
            title={title}
            text={text}
            linkApple={link}
            onClickApple={onClick}
            imageSrc={image}
            qrCode={qrCode}
            deviceMask="black-iphone"
        />
    </ContentArea>
</Playground>

### 2 стора

<Playground>
    <ContentArea>
        <StoreBanner
            title={title}
            text={text}
            linkApple={link}
            onClickApple={onClick}
            linkGoogle={link}
            onClickGoogle={onClick}
            imageSrc={image}
            qrCode={qrCode}
            deviceMask="black-iphone"
        />
    </ContentArea>
</Playground>

### 3 стора

<Playground>
    <ContentArea>
        <StoreBanner
            title={title}
            text={text}
            linkApple={link}
            onClickApple={onClick}
            linkGoogle={link}
            onClickGoogle={onClick}
            linkHuawei={link}
            onClickHuawei={onClick}
            imageSrc={image}
            qrCode={qrCode}
            deviceMask="black-iphone"
        />
    </ContentArea>
</Playground>

## Кнопка

### На белом фоне

<Playground>
    <ContentArea>
        <StoreBanner
            title={title}
            text={text}
            linkButton="#"
            imageSrc={image}
            deviceMask="black-iphone"
        />
    </ContentArea>
</Playground>

### На зеленом фоне

<Playground>
    <ContentArea>
        <StoreBanner
            title={title}
            text={text}
            linkButton="#"
            imageSrc={image}
            deviceMask="black-iphone"
            theme="green"
        />
    </ContentArea>
</Playground>

## HTML-теги и спецсимволы

```javascript

В свойствах title и text поддерживаются некоторые HTML-теги:
- для title: "<br>, <font color>, <a href>";
- для text: "<br>, <font color>, <a href>, <b>".
Также в обоих свойствах поддерживается спецсимвол &nbsp.

```

```javascript collapse=Демо данные

const htmlTitle ='Скачивайте&nbspмобильное приложение <font color="#731982">МегаФон</font><br>по <a href="https://moscow.megafon.ru">ссылке</a>';
const htmlText = '<a href="https://moscow.megafon.ru">Скачай</a>&nbspи<br><font color="#731982">получи</font> <b>максимум</b>';

```

<Playground>
    <ContentArea>
        <StoreBanner
            title={htmlTitle}
            text={htmlText}
            linkApple={link}
            onClickApple={onClick}
            linkGoogle={link}
            onClickGoogle={onClick}
            imageSrc={image}
            deviceMask="android"
        />
    </ContentArea>
</Playground>
