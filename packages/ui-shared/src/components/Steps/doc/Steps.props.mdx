import { Props } from 'docz';
import Steps from '../Steps';

<Props of={Steps} />

```javascript

interface IStepsItem = {
    /** Номер элемента */
    index: number;
    /** Текст */
    text: string;
};

```
