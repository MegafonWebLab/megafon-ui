import { Props } from 'docz';
import TextWithIcon from '../TextWithIcon';

<Props of={TextWithIcon} />

``` typescript

interface IItem {
    /** Текст */
    text: string;
    /** Иконка */
    icon: React.ReactNode;
}

```
