import { Props } from 'docz';
import PageTitle from '../PageTitle';

<Props of={PageTitle} />

``` javascript

type ItemType = {
    /** Текст хлебной крошки */
    title: string;
    /** Ссылка хлебной крошки */
    href?: string;
}

```
