---
name: VideoBanner
menu: Constructor
route: components/video-banner
---

import { Playground, Props } from 'docz';
import VideoBanner from './VideoBanner';
import { content, image, video, youtubeVideoId } from './VideoBanner.docz.tsx'

# VideoBanner

## Properties

<Props of={VideoBanner} />

## Included props

``` javascript

interface IContent {
    title: string;
    description: string;
    buttonTitle: string;
    href?: string;
    onButtonClick?: (e: React.SyntheticEvent<EventTarget>) => void;
}

interface IImage {
    mobile: string;
    desktop: string;
}

const content: IContent = {
    title: 'Текст ≈40 симовлов. Короткие слова',
    description: 'Описание должно быть примерно не более 130 символов. Пишите содержательно, кратно и не будет проблем с текстовым контентом.',
    href: '#',
    buttonTitle: 'Текст в кнопке',
    onButtonClick: () => alert('button click'),
}

const image: IImage = {
    desktop: 'image-desktop.png',
    mobile: 'image-mobile.png'
}

const videoSrc = 'video.mp4' | '2Sps5MnvlKM';

const videoType = 'video' | 'youtube';
```

## Basic usage

<Playground>
    <VideoBanner videoType="video" videoSrc={video} image={image} />
</Playground>

## With content

<Playground>
    <VideoBanner videoType="video" videoSrc={video} image={image} content={content} />
</Playground>

## Only image

<Playground>
    <VideoBanner image={image} content={content} />
</Playground>

## Youtube

<Playground>
    <VideoBanner videoType="youtube" videoSrc={youtubeVideoId} image={image} />
</Playground>
